export interface Product {
    sku: string,
    name: string,
    description: string,
    unitPrice: number,
    active: boolean,
    unitsInStock: number,
    dateCreated: Date,
    lastUpdated: Date    
}
